<% if @stream_with_error.present? %>
	$.notify('error occured while importing OPML', 'error')
<% else %>
	var html = "<%= escape_javascript(
		render('home/streams', :collection => @streams)) %>"
	$("#streams_list").html(html)
	$.notify('OPML successfully imported!', 'success')
<% end %>
