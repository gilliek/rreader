<% if @rss_stream.errors.any? %>

<% else %>
  $("#new_subscription").dialog("close");

  var html = "<%= escape_javascript(
           render('home/streams', :collection => @streams)) %>"
  $("#streams_list").html(html)
  $.notify("RSS/Atom feed successufuly added !", "success")
<% end %>
