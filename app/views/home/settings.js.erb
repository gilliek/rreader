var html = "<%= escape_javascript(render('settings')) %>"
$("#content").html(html);

// add the pill change effect on click
$("ul#settings-pills li a").click(function() {
 $("ul#settings-pills li").removeClass("active");
 $(this).parent().addClass("active");
});
